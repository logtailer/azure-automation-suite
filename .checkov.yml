directory:
  - terraform

framework:
  - terraform

skip-check:
  - CKV_AZURE_35   # Storage account public access — dev only
  - CKV_AZURE_110  # Function app HTTPS only — not in scope
  - CKV_AZURE_4    # AKS OMS logging — enabled via container insights vars
  - CKV_AZURE_10   # SSH unrestricted — demo VM, not production
  - CKV_AZURE_117  # AKS disk encryption set — not configured in sample
  - CKV_AZURE_160  # HTTP unrestricted — demo VM, not production
  - CKV_AZURE_168  # AKS min 50 pods — architecture decision per node pool
  - CKV_AZURE_172  # AKS secrets CSI autorotation — not configured in sample
  - CKV_AZURE_227  # AKS temp disk encryption — not configured in sample
  - CKV_AZURE_50   # VM extensions — demo VM, not production
  - CKV2_AZURE_1   # Storage CMK — sample velero storage account
  - CKV2_AZURE_21  # Storage blob logging — sample velero storage account
  - CKV2_AZURE_33  # Storage private endpoint — sample velero storage account
  - CKV2_AZURE_38  # Storage soft-delete — sample velero storage account
  - CKV2_AZURE_40  # Storage shared key auth — sample velero storage account
  - CKV2_AZURE_41  # Storage SAS expiration — sample velero storage account
  - CKV2_AZURE_47  # Storage blob anonymous access — sample velero storage account
  - CKV_AZURE_119  # NIC public IP — demo VM, not production
  - CKV_AZURE_120  # App Gateway WAF — not configured in sample
  - CKV2_AZURE_31  # Subnet NSG — bastion, DNS resolver, delegated subnets exempt by design
  - CKV2_AZURE_26  # PostgreSQL firewall allow-azure-services rule — intentional for managed services
  - CKV2_AZURE_56  # MySQL private endpoint — not required in sample
  - CKV2_AZURE_28  # Container group managed identity — pgbouncer sidecar, not in scope
  - CKV2_AZURE_57  # PostgreSQL private endpoint — read replica uses delegated subnet by design
  - CKV_AZURE_109  # Key vault firewall — access restricted via private endpoint instead
  - CKV_AZURE_114  # Key vault secret content_type — not required for platform secrets
  - CKV_AZURE_20   # Security contact phone — not configured in sample
  - CKV_AZURE_216  # Firewall threat intel mode — alert mode intentional for dev
  - CKV_AZURE_220  # Firewall policy IDPS deny — not configured in sample
  - CKV_AZURE_41   # Key vault secret expiration — managed via rotation policy
  - CKV2_AZURE_35  # Recovery vault managed identity — not configured in sample
  - CKV_AZURE_218  # App gateway TLS protocols — sample uses HTTP listener for lab simplicity
  - CKV_AZURE_217  # App gateway HTTP listener — sample only, HTTPS enforced in prod tfvars
  - CKV_AZURE_136  # PostgreSQL geo-redundant backup — disabled for dev cost savings
  - CKV_AZURE_137  # ACR admin account — disabled for Basic SKU sample
  - CKV_AZURE_139  # ACR public network — Basic SKU, private endpoint not available
  - CKV_AZURE_163  # ACR vulnerability scanning — requires Defender for Containers (paid)
  - CKV_AZURE_164  # ACR trusted images — content trust disabled for Basic SKU
  - CKV_AZURE_165  # ACR geo-replication — disabled for cost savings
  - CKV_AZURE_166  # ACR image quarantine — not supported in Basic SKU
  - CKV_AZURE_167  # ACR retention policy — disabled in Basic SKU
  - CKV_AZURE_233  # ACR zone redundancy — not available in Basic SKU
  - CKV_AZURE_235  # Container env vars — sample only, secrets via Key Vault in prod
  - CKV_AZURE_237  # ACR dedicated data endpoints — not supported in Basic SKU
  - CKV_AZURE_245  # Container group VNet — pgbouncer sidecar, VNet integration in prod
  - CKV_AZURE_98   # Container group VNet — duplicate of CKV_AZURE_245
  - CKV_AZURE_170  # AKS Paid SKU SLA — Free tier intentional for dev
  - CKV_AZURE_141  # AKS local admin disabled — required for dev access
  - CKV_AZURE_115  # AKS private cluster — not required for CI runner cluster
  - CKV_AZURE_7    # AKS network policy — azure CNI handles isolation
  - CKV_AZURE_232  # AKS system node critical pods only — single pool in CI cluster
  - CKV_AZURE_226  # AKS ephemeral OS disks — not configured in sample
  - CKV_AZURE_6    # AKS API server authorized IPs — controlled via Azure RBAC
  - CKV_AZURE_171  # AKS upgrade channel — manual upgrades in dev
  - CKV_AZURE_206  # Storage replication — LRS intentional for dev cost savings
  - CKV_AZURE_33   # Storage queue logging — not required for Nexus file share
  - CKV_AZURE_59   # Storage public access — Nexus data store, scoped to VNet in prod
  - CKV_AZURE_244  # Storage local users — not used, managed identity auth only
  - CKV_AZURE_36   # Storage trusted Microsoft services — dev bootstrap account
  - CKV_AZURE_3    # Storage HTTPS only — enforced at policy level
  - CKV_AZURE_34   # Storage blob public access — disabled at account level
  - CKV_AZURE_182  # VNET DNS endpoints — Azure default DNS used in dev
  - CKV_AZURE_183  # VNET local DNS — Azure default DNS used in dev
  - CKV_AZURE_9    # NSG RDP from internet — demo NSG rules, scoped in prod
  - CKV_AZURE_77   # NSG UDP from internet — demo NSG rules, scoped in prod
  - CKV_AZURE_118  # NIC IP forwarding — load balancer NICs require forwarding
  - CKV_AZURE_84   # Defender for Storage — disabled in dev for cost savings
  - CKV_AZURE_85   # Defender for Kubernetes — disabled in dev for cost savings
  - CKV_AZURE_87   # Defender for Key Vault — disabled in dev for cost savings
  - CKV_AZURE_86   # Defender for Container Registry — disabled in dev for cost savings
  - CKV_AZURE_79   # Defender for SQL on machines — disabled in dev for cost savings
  - CKV_AZURE_69   # Defender for SQL databases — disabled in dev for cost savings
  - CKV_AZURE_55   # Defender for Servers — disabled in dev for cost savings
  - CKV_AZURE_61   # Defender for App Service — disabled in dev for cost savings
  - CKV_AZURE_234  # Defender for Resource Manager — disabled in dev for cost savings
  - CKV_AZURE_19   # Security Center standard tier — Free tier intentional for dev
  - CKV_AZURE_44   # Storage TLS version — bootstrap state account, enforced via policy
  - CKV_AZURE_190  # Storage blob public access — disabled at account level

compact: true
quiet: false
