# 07-idp

## Purpose
This component manages identity platform resources for Azure.

## Usage
- Edit `core.tfvars` with your values.
- Run `deploy.sh` or use Terraform CLI.

## Variables
- Add IDP-specific variables in `variables.tf`.

## Outputs
- Add outputs in `outputs.tf` as needed.
