# 05-observability

## Purpose
This component manages observability resources for Azure.

## Usage
- Edit `core.tfvars` with your values.
- Run `deploy.sh` or use Terraform CLI.

## Variables
- Add observability-specific variables in `variables.tf`.

## Outputs
- Add outputs in `outputs.tf` as needed.
