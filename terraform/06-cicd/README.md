# 06-cicd

## Purpose
This component manages CI/CD resources for Azure.

## Usage
- Edit `core.tfvars` with your values.
- Run `deploy.sh` or use Terraform CLI.

## Variables
- Add CI/CD-specific variables in `variables.tf`.

## Outputs
- Add outputs in `outputs.tf` as needed.
