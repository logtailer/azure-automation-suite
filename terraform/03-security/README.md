# 03-security

## Purpose
This component manages security resources for the Azure environment.

## Usage
- Edit `core.tfvars` with your values.
- Run `deploy.sh` or use Terraform CLI.

## Variables
- Add security-specific variables in `variables.tf`.

## Outputs
- Add outputs in `outputs.tf` as needed.
